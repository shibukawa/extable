{
  "name": "@extable/docs",
  "private": true,
  "version": "0.3.4",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev .",
    "prebuild": "node -e \"const {execSync}=require('child_process'); execSync('npm run build --workspace @extable/core --if-present',{stdio:'inherit'}); execSync('npm run build --workspace @extable/vue --if-present',{stdio:'inherit'});\"",
    "build": "vitepress build .",
    "preview": "vitepress preview ."
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^6.0.3",
    "vite": "^7.3.1",
    "vitepress": "^1.6.4",
    "vitepress-plugin-tabs": "^0.7.3",
    "vue": "^3.5.26"
  },
  "dependencies": {
    "@extable/core": "0.3.4",
    "@extable/vue": "0.3.4",
    "vue": "^3.5.26"
  }
}
